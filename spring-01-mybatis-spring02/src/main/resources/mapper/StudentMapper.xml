<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ta.mapper.StudentMapper">
    

    <!--方式一 按照结果嵌套处理-->
    <select id="selectStudents" resultMap="studentTeacher">
      SELECT s.id,s.`name` AS student_name,t.id as teacher_id,t.`name` AS teacher_name from student s LEFT JOIN teacher t ON s.tid = t.id;
    </select>

    <resultMap id="studentTeacher" type="Student">
        <result property="id" column="id"/>
        <result property="name" column="student_name"/>
        <association property="teacher" javaType="Teacher">
            <result property="id" column="teacher_id"/>
            <result property="name" column="teacher_name"/>
        </association>
    </resultMap>


    <!--方式二 子查询-->
    <select id="selectStudents2" resultMap="studentTeacher2">
      select * from student;
    </select>

    <resultMap id="studentTeacher2" type="Student">
        <!--<result property="id" column="id"/>-->
        <!--<result property="name" column="name"/>-->
        <association property="teacher" column="tid" javaType="Teacher" select="selectTeachers">
            <!--<result property="id" column="teacher_id"/>-->
            <!--<result property="name" column="teacher_name"/>-->
        </association>
    </resultMap>

    <select id="selectTeachers" resultType="Teacher">
        select * from teacher where id = #{id}
    </select>

</mapper>