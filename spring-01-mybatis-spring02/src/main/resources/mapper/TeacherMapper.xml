<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ta.mapper.TeacherMapper">
    <select id="selectTeachers" resultType="Teacher">
      select * from teacher;
    </select>

    <select id="selectTeachers1" resultMap="selectTeachers1">
      SELECT t.id,t.`name` AS teacher_name,s.id AS student_id,s.`name` AS student_name FROM teacher t LEFT JOIN student s ON t.id = s.tid WHERE t.id = #{id};
    </select>


</mapper>